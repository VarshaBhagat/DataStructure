// https://www.geeksforgeeks.org/find-length-of-a-linked-list-iterative-and-recursive/
function sizeOfList(listHead) {
    if(listHead=== null) return 0;
    if(listHead!=null){
        console.log(list)
        return 1+sizeOfList(listHead.next)
    }
}

// sizeOfList(list.head)